{
  "name": "ship-tracker",
  "version": "1.0.0",
  "description": "AIS geofence notifier — alerts via Pushover, Telegram, and webhook when a tracked ship enters a zone",
  "main": "tracker.js",
  "scripts": {
    "start": "node tracker.js",
    "dev": "node --watch tracker.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.0",
    "globals": "^17.5.0"
  }
}
